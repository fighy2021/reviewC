(a) : 
 因為 當我們在輸入完字元，並按下enter鍵時，
  系統會將enter鍵判斷為 carrage return(歸位) 及 line feed(換行) 這兩個動作。
 所以 當scanf()函式在收到 歸位 字元時，會判斷資料已輸入完畢，這本身沒什麼問題；
  但問題是，這時的 換行 字元還在緩衝區內，
 那麼 當程式運行到第十一行時，會因為讀取到了還在緩衝區內的 換行字元，
  並判斷資料已輸入完畢，然後直接運行下一行。
(b) : 將第十一行修改為 : 
 scanf(" %c", &ch2);